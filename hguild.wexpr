@(
	package "libWexpr"
	buildMethod "catalyst"

	dependencies #(
		@(type "repository" name "Catalyst")
		@(type "repository" name "External-Curl")
	)
	
	runCommands @(
		WexprTool_gdb @(command "gdb --args ${INSTALL_PATH}/Tools/WexprTool ${ARGS}")
		WexprTool @(command "${INSTALL_PATH}/Tools/WexprTool ${ARGS}")
	)
)
